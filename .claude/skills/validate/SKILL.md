---
name: validate
description: "DEPRECATED — Use /dev-validate-all instead. This skill redirects to the full validation pipeline."
---

**This skill is deprecated. Use `/dev-validate-all` instead.**

Run `/dev-validate-all` for the complete validation pipeline (tsc + lint + test + custom checks + build).
