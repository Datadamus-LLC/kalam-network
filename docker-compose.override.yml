# Development overrides — automatically applied by `docker compose up`
# This file adds dev-only tools. Do NOT deploy to production.

services:
  postgres:
    ports:
      - "${DB_PORT:-5432}:5432"

  redis:
    ports:
      - "${REDIS_PORT:-6382}:6379"
